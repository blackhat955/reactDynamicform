import React from 'react';
import "./input.css";
import Select from './option.js';
const selectInput=(props)=>{
  return(
    <div>
    <select>
    <Select/>
    </select>
    </div>

  );
}
export default selectInput;